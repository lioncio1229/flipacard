import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            light: "#E8F9FF",
            main: "#155a68",
            dark: "#155a68"
        },
        secondary: {
            light: "#ffa518",
            main: "#FF7A0D",
            dark: "#b95300",
        }
    }
});

export default theme;

